# نشر الداشبورد (GitHub + Vercel)

هذه نسخة **ويب داشبورد** من نظام "الراشد للأنظمة" الأصلي (Electron)، مبنية من نفس كود الواجهة والمنطق بالكامل (كل الصفحات: الكاشير، المنتجات، المخزون، العملاء، الموردون، المشتريات، المالية، التقارير، الصيانة/الريبير، المستخدمون، الفروع...). تعمل مباشرة في المتصفح وتتصل بنفس قاعدة بيانات Supabase.

## ما الذي تغيّر عن نسخة سطح المكتب؟
- تمت إزالة مجلد `electron/` والطباعة الحرارية عبر USB وقاعدة sqlite المحلية (offline sync) لأنها خاصة بتطبيق سطح المكتب فقط.
- كل استدعاءات `window.electronAPI` في الكود الأصلي كانت بالفعل محمية بشرط (`?.`)، فتتحول الطباعة تلقائيًا لنافذة طباعة المتصفح العادية بدل الطابعة الحرارية، وتعمل كل البيانات مباشرة أونلاين عبر Supabase بدل التخزين المحلي.
- بقيت كل صفحات ومنطق العمل (business logic) كما هي 100% بدون حذف أي ميزة.

## 1) الرفع على GitHub
```bash
cd rashed-systems-pos-web
git init
git add .
git commit -m "Web dashboard version"
git branch -M main
git remote add origin <رابط-الريبو-بتاعك>
git push -u origin main
```

## 2) النشر على Vercel
1. ادخل على vercel.com → New Project → استورد الريبو من GitHub.
2. Vercel هيكتشف إنه مشروع Vite تلقائيًا (فيه ملف `vercel.json` جاهز أصلاً بـ build command و output directory).
3. لا تحتاج تضيف أي Environment Variables — بيانات اتصال Supabase (URL + Publishable/Anon Key) مكتوبة في `src/lib/supabase.js` بنفس الطريقة اللي كانت بيها في نسخة سطح المكتب (المفتاح ده عمومًا آمن للنشر العام طالما عندك RLS policies مفعّلة في Supabase).
4. اضغط Deploy، وخلال دقيقة أو اتنين هيديك رابط مباشر للداشبورد.

## 3) التجربة محليًا قبل الرفع (اختياري)
```bash
npm install
npm run dev
```
هيفتح على `http://localhost:5173`.

## ملاحظات مهمة
- تسجيل الدخول لسه عبر Supabase Auth بنفس المستخدمين الموجودين.
- الفروع، الصلاحيات (مدير/مشرف/كاشير)، عزل البيانات لكل فرع — كله شغال زي ما هو.
- لو حابب لاحقًا تضيف PWA / offline support لنسخة الويب، ده يحتاج شغل إضافي منفصل عن اللي هنا.
