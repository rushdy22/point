# البنية Offline-First (SQLite محلي + مزامنة تلقائية مع Supabase)

## الفكرة العامة
كل قراءة وكتابة في التطبيق تتم فورًا من/إلى قاعدة بيانات **SQLite محلية**
(`better-sqlite3`) داخل جهاز الكاشير — سريعة جدًا ولا تنتظر الإنترنت أبدًا.
في الخلفية، محرك مزامنة (`electron/sync/engine.js`) يدفع أي تغييرات جديدة إلى
Supabase كل ما توفر اتصال، ويسحب أي تغييرات جاءت من أجهزة/فروع أخرى.

**لم يتم تعديل أي صفحة أو منطق عمل (business logic) موجود.** كل ملفات
`src/lib/db/*.js` (المنتجات، المبيعات، العملاء، الموظفين، الحسابات...) تعمل
**بدون أي تغيير** لأنها تستخدم نفس واجهة `supabase.from('جدول').select()...`
المعتادة تمامًا — الفرق الوحيد أن `src/lib/supabase.js` أصبح "يوجّه" هذه
الاستدعاءات لقاعدة SQLite محليًا عبر IPC بدل الشبكة مباشرة.

## المكونات
```
electron/db/localSchema.js   مخطط SQLite (نفس جداول Supabase + أعمدة مزامنة)
electron/db/tableConfig.js   وصف كل جدول: أعمدته، علاقاته (JOIN)، ترتيب المزامنة
electron/db/queryEngine.js   يترجم أي استعلام (select/insert/update/delete/upsert) إلى SQL محلي
electron/db/sqlite.js        يفتح/ينشئ ملف قاعدة البيانات المحلية
electron/sync/engine.js      محرك المزامنة: فحص الاتصال، الدفع، السحب، إعادة المحاولة
electron/main.js             يربط كل ده بـ IPC + يبعت حالة الاتصال للواجهة
src/lib/supabase.js          الغلاف (shim) اللي بيخلي كل كود lib/db القديم يشتغل من غير تعديل
```

## مؤشر الاتصال
شارة صغيرة أعلى كل صفحة:
- 🟢 متصل (Online)
- 🟡 جارٍ المزامنة (Syncing)
- 🔴 غير متصل (Offline) — التطبيق يستمر في العمل بشكل طبيعي بالكامل

## حالة كل سجل (Sync Status)
كل صف في أي جدول محلي معاه أعمدة داخلية (`_sync_status`, `_dirty`,
`_retry_count`, `_last_error`) بقيم: `pending` (لسه محتاج يترفع)،
`synced` (اترفع بنجاح)، `failed` (فشلت آخر محاولة، هيعاد المحاولة تلقائيًا
في الدورة الجاية كل ~6 ثوانٍ). هذه الأعمدة داخلية فقط ولا تظهر أبدًا في
البيانات المرسلة للواجهة.

## أهم قرار تصميمي: تفادي ازدواج خصم المخزون
عند إتمام عملية بيع، الكود الحالي (أونلاين) كان يعتمد على **Trigger في
Postgres** يخصم المخزون تلقائيًا فور إدخال سطر `sale_items`. في وضع
Offline-First، لازم الخصم يحصل **فورًا محليًا** (عشان الكاشير يشوف الكمية
الجديدة فورًا من غير ما ينتظر اتصال)، لكن من غير ما نرفع تعديل منفصل لعمود
`stock_quantity` كتغيير "pending" — لأن رفعه هيخلي الـ Trigger في Supabase
يخصم **مرة تانية** (ازدواج). الحل: خصم المخزون محليًا مباشرة (تحديث فوري
بدون وضعه في طابور المزامنة)، وترك سطر `sale_items` نفسه فقط هو اللي بيتزامن؛
الـ Trigger البعيد بيطبّق نفس الخصم مرة واحدة بس، وبعد كده أي جهاز (بما فيه
هذا الجهاز) بيسحب الرقم الرسمي الصحيح تلقائيًا في دورة المزامنة الجاية.

أي تعديل يدوي على المخزون (تسوية جرد، نقل بين فروع، استرجاع) **مفيهوش
Trigger مماثل**، فبيتزامن بشكل طبيعي زي أي تعديل عادي.

## استراتيجية السحب (Pull)
- جداول صغيرة (الفروع، الأقسام، المستخدمين، الموظفين، نسب عمولاتهم): يتم
  تحديثها بالكامل كل دورة مزامنة (رخيصة وبسيطة).
- جداول أكبر (المنتجات، العملاء، المبيعات وتفاصيلها، حركات المخزون،
  المعاملات المالية...): تُسحب تدريجيًا فقط بالتغييرات الجديدة (بالاعتماد
  على `updated_at`/`created_at`)، لتفادي إعادة تحميل كل البيانات كل مرة.
- تعارض التعديلات: لو نفس السجل اتعدل محليًا ولسه ماتزامنش، وفي نفس الوقت
  جالنا تحديث من السيرفر، بيفوز التعديل المحلي (لحد ما يترفع بنجاح)، بعدها
  السحب العادي بيحصل زي المعتاد.

## Authentication والمستخدمون
- تسجيل الدخول يستخدم `username + password` فقط. بعد أول دخول ناجح تُحفظ
  بيانات التحقق محليًا كـ scrypt hash، لذلك يعمل الدخول لاحقًا بدون إنترنت.
- إنشاء/تعديل/تعطيل/حذف المستخدم يُثبت في SQLite أولًا. كلمة المرور لا تُحفظ
  كنص صريح: يوجد hash محلي، ونسخة provisioning مشفرة داخل Outbox مؤقتة
  حتى ينفذها RPC server-side عند عودة الاتصال.
- حذف المستخدم Tombstone (`deleted_at` + تعطيل Auth) حتى لا يعيده Pull من
  جهاز آخر.
- سجل حركة البيع يُنشأ محليًا مع البيع، وخصم المخزون محلي وفوري؛ مزامنة سطر
  البيع وPull للكمية الرسمية لا يعيدان الخصم.

## التثبيت (مهم)
`better-sqlite3` مكتبة Native لازم تتبني ضد نفس نسخة Electron:
```bash
npm install
```
سكريبت `postinstall` هيشغّل `electron-rebuild` تلقائيًا. لو حصلت مشكلة في
البناء، تأكد إن عندك أدوات بناء C++ مثبتة (على Windows: "Desktop development
with C++" من Visual Studio Build Tools، أو على macOS: Xcode Command Line
Tools، أو على Linux: `build-essential` + `python3`).

## قبل التشغيل: شغّل migration_v5.sql ثم migration_v17_offline_sync_auth.sql
`sql/migration_v5.sql` بيضيف (بأمان، بدون حذف أي بيانات):
1. جداول الموظفين/العمولات لو مش موجودة أصلاً (idempotent).
2. عمود `updated_at` + Trigger على `customers` و `sales` — ضروري عشان
   محرك المزامنة يقدر يسحب التحديثات الجديدة بكفاءة (مش بس الإدخالات الجديدة).
